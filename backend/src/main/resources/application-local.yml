# Optional local run without PostgreSQL; file-backed relational H2.
spring:
  datasource:
    url: jdbc:h2:file:./data/delivery-zone;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
